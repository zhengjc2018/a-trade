# 2026-07-25 T 胜率优化与每日复盘 STATUS

- **总体状态：** 进行中
- **当前阶段：** Task 9 全量回归与配置迁移
- **当前步骤：** 全量 pytest、ruff、配置冒烟和本地 stale monitor 清理
- **已完成：**
  - 审计现有 5 分钟信号、双阶段确认、自动执行和调度流程
  - 确认按股可配置锁利/止损，默认 +3% / -2%
  - 确认大盘采用 MA20/MA60 + 5 日跌幅双闸门
  - 确认每日复盘于 15:35 推送
  - 识别个股趋势必须使用日 K，不能复用 5 分钟 K 的 MA20/MA60
  - 识别 holdings 必须继续作为实际做 T 股票唯一来源，monitor symbols 仅作阈值覆盖
  - 完成实施计划 `docs/superpowers/plans/2026-07-25-t-win-rate.md`
  - 实现日线个股趋势：MA20 > MA60 且 MA20 五日向上
  - 实现沪深 300 双闸门与 5 分钟 TTL 缓存
  - 保护 STOP_LOSS/锁利信号不受大盘闸门阻断
  - 验证：`12 passed`，`ruff check` 全部通过
  - 实现 `data/cache/t_state.json` 原子状态存储与损坏恢复
  - 增加跨日自动隔离、09:30 显式重置支持和峰值单向更新
  - 验证：状态模块 `10 passed`，`ruff check` 全部通过
  - 实现全局默认 +3%/-2% 和按股覆盖合并
  - 锁利/止损精确边界、退出手数上限和非持仓状态保护
  - 验证：风险动作 `16 passed`，`ruff check` 全部通过
  - monitor 支持全局默认和 `{symbol, trailing}` 最小覆盖结构
  - 网页卡片与新增表单支持锁利/止损百分比，空值回退默认
  - API 仅允许修改真实 holdings，并在写入时清理 stale 股票覆盖
  - 补齐 FastAPI/Uvicorn/httpx 依赖清单
  - 验证：配置与 Web `49 passed`，`ruff check` 全部通过
  - scheduler 始终从 holdings 派生做 T 股票，monitor symbols 只叠加阈值
  - 个股日线/大盘双闸门接入普通信号，风险退出始终放行
  - 锁利/止损信号首轮直出并优先于引擎普通信号
  - BUY 成功后写 holding，锁利后 locked，其他卖出后 empty
  - 成交记录保留 factor_hits/risk_action，成功 BUY 不再误标 skipped
  - 验证：监控/执行/调度 `48 passed`，`ruff check` 全部通过
  - 实现同股同日 BUY→SELL/STOP_LOSS 的 FIFO 配对与部分成交
  - 统计胜率、盈亏比、平均收益及按股票/因子聚合
  - 验证：复盘统计 `10 passed`，`ruff check` 全部通过
  - 复盘结论置顶，最多 5 条明细并明确未扣手续费/滑点
  - 收盘日报改为 15:35，复盘合并在持仓概览前，不新增独立消息
  - 09:30 显式重置 T 状态，15:40 对合并收盘报告做漏发检查
  - 验证：报告/调度/恢复 `19 passed`，`ruff check` 全部通过
- **下一步：** 执行全量回归，迁移本地/VPS stale monitor symbols
- **阻塞项：** GitHub HTTPS 瞬时不可用（已尝试 HTTP/2、HTTP/1.1；VPS 同步正常，最终阶段继续重试）
- **阻塞项：** 无
- **最后更新时间：** 2026-07-25
